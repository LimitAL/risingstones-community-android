# 官方接口兼容性矩阵

状态含义：

- `历史记录`：早期 WebView Cookie 流程曾验证，仅供参考。
- `单元测试`：请求结构已在本地验证，但未使用真实账号。
- `当前匿名`：已在当前官方服务中以无凭证方式验证。
- `当前鉴权`：已用独立客户端 WebView 创建的真实会话验证。
- `待验证`：当前证据不足。

本仓库不得提交凭证、Cookie、包含设备标识扩展的 User-Agent，或含个人数据的官方原始响应。

| 能力 | 接口或流程 | Cookie 与 User-Agent 证据 | 客户端策略 |
| --- | --- | --- | --- |
| 会话校验 | `GET /api/home/GHome/isLogin` | 2026-09-18 当前 Android 鉴权与冷启动恢复；`10002` 仅合成测试 | 只确认会话，不授予功能能力 |
| 用户摘要 | `GET /api/home/userInfo/getUserInfo` | 2026-09-18 当前鉴权 | 成功后授予 `AccountRead` |
| 签到奖励 | `GET /api/home/sign/signRewardList` | 历史记录 | 待在线验证 |
| 签到记录 | `GET /api/home/sign/mySignLog` | 历史记录 | 待在线验证 |
| 每日签到 | `POST /api/home/sign/signIn` | 历史记录 | 待在线验证，不自动探测 |
| 论坛列表、详情、评论和搜索 | 公开接口 | 2026-07-27 当前匿名 | 不需要鉴权能力 |
| 帖子排序、精华及攻略分类 | `postsList`、`partList?type=2` | 2026-09-18 当前匿名、本机无头浏览器 | 原生支持分类层级、筛选及 `pageTime` 分页 |
| 点赞与收藏 | `POST /api/home/posts/like`、`star` | 公开源码、单元及合成设备测试 | 原生入口受 `ForumWrite` 约束；真实写入待验证 |
| 回复、删除与投票 | `posts/comment`、`deleteComment`、`vote` | 公开源码、单元及合成设备测试 | 明确提交，表单请求；结果可见、已参与或截止后不再投票；真实写入待验证 |
| 评论图片上传 | `getCOSTokenI?channel=default` 与官方对象存储 | 公开源码、请求夹具及合成设备测试 | 单图和纯图片评论；独立 `ForumImageUpload` 门槛；真实上传待验证 |
| 副本招募列表、详情和目录 | `/recruitFbList`、`/getRecruitFbDetail` 等 | 2026-09-20 当前 Android 匿名：目录、队伍与位置筛选、非空列表及详情 | 不需要鉴权能力 |
| 新人、其他和跑团招募 | 官方招募接口 | 2026-07-27 当前匿名 | 不需要鉴权能力 |
| 跑团成员、评论和评分 | 官方招募接口 | 2026-09-20 当前 Android 匿名：详情、成员摘要、评论及评分排序；续页与楼中楼另有合成测试 | 不需要鉴权能力 |
| 我的部队招募 | 官方部队招募接口 | 待当前鉴权验证 | 验证后授予 `RecruitmentAuthenticated` |
| 招募回应与点赞 | 官方招募写接口 | 公开源码、单元及合成设备测试；真实写入待验证 | `RecruitmentWrite` 独立约束，回应还需作者资格明确为非本人 |
| 投影台列表、详情和目录 | 官方投影台接口 | 2026-09-18 当前 Android 鉴权：社区、关注、标签筛选及详情；种族/部族/标签结构检查成功 | 验证后授予 `GlamourAuthenticated`；游标续页 |
| 投影台本人及作者作品 | `myGlamoursList`、`myGlamoursNum`、作者公开收藏夹 | 2026-09-18 当前 Android 原生读取：本人作品为空、作者作品非空、作者收藏显示空态 | 本人不传 `uuid`，作者显式传入；不读取私有收藏 |
| 投影台物品搜索 | `searchEquip`、`getGlassesList`、`getOrnamentList` | 2026-09-18 当前 Android 鉴权：三类目录非空；装备候选选择及作品结果原生验证成功 | 保留眼镜款式分组；按编号与物品类型查询作品 |
| 投影台本人收藏夹读取 | `glamour/myFavoritesList` | 2026-09-18 当前 Android 鉴权结构检查非空 | 收藏选择器只读取本人目录 |
| 投影台收藏夹修改、点赞和收藏 | 官方投影台写接口 | 公开源码、单元测试；收藏管理和指定收藏另有合成设备测试 | 用户明确提交；真实写入仍待验证 |
| 投影台优惠券领取 | `glamourFashion/claimCoupon` | 公开源码、单元及合成设备测试 | 未关注时明确提示关注并领取；真实写入仍待验证 |
| 个人数据身份和开放状态 | `/getCharacterBindInfo`、`/dataOpenStatus` | 2026-09-18 当前鉴权 | 两项探测都成功后授予 `PersonalData` |
| 个人数据看板和详情 | 官方 `/api/home/dataCenter/*` 接口 | 单元测试 | 获得 `PersonalData` 后使用 |
| 蜃景及历史 | `getMKD*`、`getMKDIHistory6` | 2026-09-18 当前 Android 鉴权：总览、辅助职业、稀有道具及半魂晶历史 | 复用 `PersonalData`；开放标志 `mkd`；历史显式读取 |
| 朝圣交错路及历史 | `getDD*`，`dd4`、`1311` | 2026-09-18 当前鉴权结构检查为空；原生开放标志为未开放 | 按 `page_a` 提示未开放；非空记录采用公开源码与合成夹具 |
| 动态读取 | 关注流、详情、评论及楼中楼 | 2026-09-18 关注流、详情与空评论当前鉴权；楼中楼使用合成夹具 | 探测关注流成功后授予 `DynamicRead` |
| 动态正文点赞、评论与本人内容删除 | 官方 `dynamic/like`、`comment`、`deleteDynamic`、`deleteComment` | 2026-09-22 公开源码、合成接口及设备验证；真实写入待验证 | 用户明确操作成功后独立授予 `DynamicWrite`；删除须有官方作者归属 |
| 动态评论单图上传 | `getCOSTokenI?channel=default` 与官方 COS | 2026-09-22 合成传输、隔离与图片设备验证；真实上传待验证 | 独立授予 `DynamicImageUpload`，不授予动态写入能力 |
| 个人主页、发布与收藏历史、关注 | `userInfo`、`userRelation/followList` | 2026-09-18 当前鉴权：本人资料和收藏；发布历史与他人资料另有浏览器证据 | 复用 `AccountRead`；数据层剔除未公开字段 |
| 粉丝列表 | `userRelation/fansList` | 公开源码与合成夹具 | 仅显式打开时读取；可能确认新粉丝提醒，未自动线上探测 |
| 消息与未读摘要 | 官方 `/api/home/sysMsg/*` | 2026-09-18 浏览器鉴权；部队招募消息返回 `10401` | 仅 `getTip` 探测 `MessageRead`，Android 已确认能力；列表由用户显式读取 |

## 当前登录流程证据

以下证据验证客户端安全与状态边界，但不等于真实账号的线上功能验证：

- 单元测试覆盖精确 Cookie 名解析、损坏凭证恢复、瞬时校验失败、重试、并发候选会话、
  OAuth 返回代数、取消和部分注销失败。
- 2026-07-27，Android 17 Pixel Fold 模拟器成功加载当前官方登录页。匿名 `10001` 或
  “请先登录”响应会保持为正常未登录状态。
- 同一模拟器的全新 Debug APK 冷启动可以加载论坛主题和公开副本招募列表；打开账号管理会
  进入应用内 WebView，未输入凭证直接关闭后仍保持未登录，进程没有崩溃。
- 设备测试确认加密文件不包含 Cookie 或 User-Agent 明文，且不完整密文记录会被删除。
- 设备测试确认断开连接只删除官方网页与接口域名下精确名为 `ff14risingstones` 的 Cookie，
  并保留其他 WebView Cookie。
- 论坛界面设备测试覆盖紧凑、中等和展开宽度，以及官方帖子内部链接的应用内导航。
- 会话管理界面设备测试确认只显示能力布尔状态，脱敏报告不包含显示名、凭证来源、Cookie
  或 User-Agent。

## 真实账号验证步骤

1. 使用专门的测试账号，在干净测试设备或模拟器上安装独立 Debug 应用。
2. 打开内置官方网页，只记录观察到的主机名，不记录完整重定向地址、查询参数、Cookie、
   账号标识或包含个人数据的截图。
3. 只在内置官方网页中完成登录，确认认证重定向最终返回石之家官方主机。
4. 使用同一 WebView User-Agent 且不添加 `authorization` 请求头，确认捕获的 Cookie 能通过
   `/api/home/GHome/isLogin`。
5. 确认 `getUserInfo` 成功，并在会话管理页看到 `AccountRead` 已验证。只保存“复制脱敏报告”
   生成的能力布尔结果。
6. 重启进程，确认 Keystore 会话能够恢复并重复两项校验。
7. 在应用中断开连接并重启，确认旧会话不再恢复；重新打开 WebView，确认目标 Cookie 已删除，
   同时其他 Cookie 不受影响。
8. 再次登录，确认即使 Cookie 字符串没有变化，认证页返回后仍会重新校验。
9. 每次只验证一个接口，只记录响应码、脱敏消息类别和成功或失败。
10. 只有成功与会话过期行为都明确后，才增加对应能力。

真实验证不会通过提交凭证的方式自动化。

## 2026-09-18 移植验证

- 本机无头移动浏览器确认 Vue 路由及帖子、攻略筛选请求。动态匿名读取返回 `10403`。
- 用户在独立浏览器手动登录后，动态列表、动态详情、动态评论、个人摘要和关系计数的
  只读请求返回 `10000`。只核对字段形状与状态，不保存账号内容。
  此证据来自浏览器会话，不等于 Android WebView 会话已经完成当前鉴权验证。
- 论坛新增分类、筛选、游标与并发防护通过本地单元测试；全仓库 Lint 和 Debug 构建通过。
- Android 17 模拟器的六项论坛 Compose 测试通过，包含 599、600、839、840dp 四个边界、
  攻略子分类与筛选、帖子内部链接。
- 安装独立 Debug 应用后，以匿名状态成功加载当前官方论坛列表和分类。
- 本批累计 381 项单元测试通过；全仓库 Lint 与 Debug 构建通过。
- 动态七项、幻化七项、招募七项、会话界面七项及认证安全四项设备测试通过；加上论坛六项，
  加上消息六项和个人主页七项，共五十一项。来源详情覆盖 599、600、839、840dp、加载与返回。
- 个人数据十一项设备测试通过，其中探索页面八项覆盖四个断点、返回、窗口变化、失败保留及
  能力撤销；加上前述五十一项，功能与安全设备验证累计六十二项。
- 投影台浏览补齐后十五项设备测试通过，比前述七项增加八项，累计功能与安全设备验证七十项。
  新增覆盖关注源与游标、筛选确认/取消、作者返回、详情失败返回及四个断点间窗口变化。
- 投影台搜索与收藏操作补齐后，二十六项设备测试全部通过，比前述十五项增加十一项，
  累计功能与安全设备验证八十一项。新增包含四个断点的候选搜索、分组、取消、指定收藏、
  收藏夹新建/编辑/删除限制、优惠券，以及 Activity 重建中的选择保持、一次提交与退出释放。
  写操作均为合成服务；真实账号只读取。
- 三十六个本地 Maven 库制品及十二种独立公共 API 消费、聚合消费编译通过。
- 跨模块响应码对齐新增四十三项单元测试；既有四项认证安全设备测试复跑通过，不重复计入
  前述八十一项。网络公共策略已通过独立 Maven 消费编译，源码门禁及负向自测通过。
- 论坛互动补齐新增五十八项单元测试，覆盖表单删除、上传凭证边界、单多选与数值截止时间、
  结果字段存在性、草稿与请求竞态、能力撤销及最终鉴权失效清理；全仓库累计三百八十一项。
  三十六个本地库和十二种独立公共 API 消费再次通过，新扩展接口与截止时间函数已纳入消费。
- 论坛模块二十八项设备测试通过，比既有六项新增二十二项，累计功能与安全设备验证一百零三项。
  新增覆盖 599、600、839、840dp、主评论与楼中楼回复参数、点赞收藏双向反馈、草稿保留/丢弃、
  错误后图片上传复用、纯图片评论、投票选择范围及禁止重复提交、21 MiB 受限读取和尺寸归一。
  同一 Activity 两次重建保留草稿与图片，待提交评论完成一次；追加覆盖读图中重建、选图器
  打开时跨宽度回调、继续回复草稿的目标保留与首次加载撤销权限后的重试。所有互动服务和图片均为合成。
- 会话复核测试覆盖能力撤销、明确失效清理、网络失败保留与取消传播。旋转时由同一运行时
  持有服务和会话，不重复执行恢复。

匿名契约测试同时覆盖当前驼峰关系字段和早期下划线字段，避免服务端响应结构切换后详情页被
静默解析为空。

- 本地 HTTP 回归确认显式登录 UA 覆盖任意大小写的默认 UA；会话恢复与刷新保留保存的 UA。
- 独立 Debug 应用在模拟器从 599dp 切换至 840dp 后保留招募目的地；这不替代真实账号验证。


## 2026-09-18 Android 真实会话补充

用户在独立 Android 客户端内置 WebView 手动完成官方登录。原生能力摘要确认
`AccountRead`、`GlamourAuthenticated`、`PersonalData`、`DynamicRead`、`MessageRead`
为真；签到、论坛写入、图片上传及招募写入仍未验证，不自动开放。

已用该会话加载原生账号摘要、个人主页、本人帖子收藏、关注动态及动态详情；动态到原生幻化
详情的跳转成功。只记录固定界面状态与能力布尔值，不保存显示名、计数、内容、图片或原始响应。
粉丝列表未打开；未执行签到、领奖、关注、点赞或其他写入。

Chrome 会话随后在基础登录和数据中心读取中返回 `10105`。不能将该结果作为蜃景或深层迷宫
响应结构证据，也不据此判定 Android 会话失效；后续结构检查与原生加载使用 Android 会话。


同次真实设备检查还确认：强制结束并冷启动应用后，原生账号摘要无需重新登录即可读取。
因此保存的 Cookie 与登录 UA 在当前 Android 会话中已通过恢复验证；未使用不同 UA 做破坏性试验。

响应码对齐后的最新 Debug 构建再次安装并强制冷启动，原生账号页和签到记录/奖励摘要仍可
直接读取，无需重新登录。该检查没有执行签到或领取；真实返回仍未观察到 `10002`，
该码的接受、重试与写入次数只由公开源码和合成回归证明。

投影台补齐后，沿用此 Android 会话验证了原生关注流、本人作品空态与统计、公开标签筛选、
作品详情、作者作品与统计、作者公开收藏入口。作者作品非空，收藏入口显示正常空态；返回后
原列表筛选和所选详情保持。七个固定投影台 GET 的设备内结构检查另行通过，均为 `10000`。
没有自动执行关注、取消关注、点赞、收藏、优惠券领取或投稿。写操作仍仅有源码和夹具证据。

搜索与收藏操作补齐后，四个固定 GET 在同一 Android 会话中均为 HTTP 200、业务码 `10000`
且非空：装备、眼镜、配饰和本人收藏夹。结构检查只输出字段类型与响应类别；眼镜描述使用
`des`，目录保留款式分组。新版原生界面以固定关键词检索装备，选中候选后作品列表非空；
没有保存候选或作品实际值。此结果不等于眼镜和配饰的原生结果流程均已完成真实账号回归，
两类界面流程目前由合成测试覆盖。新增写入界面未操作真实账号。


- Android 手动只读结构检查已确认蜃景和深层迷宫十五个请求成功，其中深层迷宫全部为空数组；
  脱敏输出负向设备测试一项通过。对应看板现已原生实现，非空深层迷宫记录尚无真实账号证据。

- 原生消息未读摘要和系统消息列表在同一 Android 会话中加载成功。此列表读取沿用用户此前
  对消息验证及清除对应未读提醒的明确授权；其他消息类别本轮未重复读取。

- 安装探索看板版本后，原有 Android 会话直接加载蜃景总览与辅助职业进度；稀有道具历史和
  半魂晶历史均有记录并正常显示。只记录加载结果布尔值，不保存角色数值或历史内容。
- 朝圣交错路的当前 `page_a` 标志未开放，原生页显示未开放状态且不自动读取子数据；之前
  十五项结构检查中的空数组不等同于拥有开放权限。接口正例、层数失败统计和职业复合字段由
  公开源码与合成夹具覆盖。

- 会话保持 Active 但移除已授予能力时，同样清除受保护的功能 ViewModel；相同能力复核或新增
  能力则保留选择。两项补充单元测试覆盖部分撤销、凭证来源变化和能力增加。

## 2026-09-20 收尾复核

仓库保留的设备结果确认论坛二十八项全部通过；重新执行全仓库单测、Lint、Debug 构建和
源码门禁通过。聚合 Maven 消费补齐对 `forum-presentation` 的显式依赖后通过；十二种独立
公共 API 消费均通过，没有向数据层加入展示依赖。

本机原有模拟器当日未运行；保留数据启动原配置并覆盖安装最终论坛构建后，公开论坛列表
正常加载。账号页显示官方登录入口，当前会话不能视为有效；不推断失效原因，不覆盖此前
9 月 18 日的有效验证记录。后续先使用匿名读取和合成互动测试，不执行真实写入。

## 2026-09-20 招募原生流程补齐

- 本机无头浏览器重新核对公开移动分块中的完整筛选、回应、作者资格、跑团评论排序与点赞参数。
  旧服务接口保留，新能力通过可选扩展提供；没有以写请求探测当前账号能力。
- 同日独立匿名浏览器的十类固定 API 结构检查返回 HTTP `567`、业务码 `10567`，未取得可用
  招募数据；依赖列表编号的详情链因此跳过。网页跨域读取另受 CORS 限制，已停止重试。
  该限制不等同于业务接口不可用，也不能替代下述原生 Android 匿名成功证据。
- 全仓库四百四十四项单元测试通过，无失败、错误或跳过；Lint、Debug 构建、源码门禁与负向
  安全自测通过，三语资源六百四十三项一致。
- 招募二十五项设备测试全部通过，比原有七项增加十八项，累计功能与安全设备验证一百二十一项。
  覆盖 599、600、839、840dp、筛选确认/取消、同区多服、换区与全服参数、回应保留/丢弃、
  三十字符限制、失败重试、空联系人成功后补读、资格未知时恢复草稿、评论排序与两级分页重试。
  Activity 重建中待提交请求只完成一次；上述写入均来自合成服务。
- 三十六个本地 Maven 库制品、十三种独立公共 API 消费和聚合消费编译通过；新增招募扩展
  已纳入单独的 `recruitment-data` 消费面，展示状态另纳入聚合消费。
- 覆盖安装最终 Debug 应用后，以匿名状态成功加载副本目录，应用队伍和位置筛选得到非空列表，
  打开详情成功。跑团列表和详情正常，成员/评论区可读，评论从最新切换到最高评分后正常加载。
  未记录条目编号、作者信息或内容，未操作回应、点赞或其他写入。
- 用户确认曾在 Android WebView 登录并回到原生账号页；本次环境复核仍显示登录入口，故本批
  不追加持续有效的鉴权结论，也不推断该变化原因。当前读写验证范围与此前历史证据分开记录。

## 2026-09-20 跑团成员与活动阅读补齐

- 本机无头浏览器核对成员完整列表、成员详情、活动列表与活动详情的官方公开脚本。成员后续页
  沿用第一页游标；活动列表无分页并保留官方顺序，成员介绍按普通文本呈现，活动按 HTML 图文
  顺序呈现。没有执行真实写入，也没有重试此前受网关挑战阻止的浏览器 API 探测。
- 全仓库四百八十二项单元测试通过，无失败、错误或跳过；Lint、Debug 构建通过。
  源码门禁及负向安全自测通过，三语资源六百五十八项一致。
- 招募三十七项设备测试全部通过，比此前二十五项新增十二项，累计功能与安全设备验证一百三十三项。
  新增覆盖 599、600、839、840dp、成员游标与去重、活动顺序和图文详情、刷新失败保留、
  父跑团切换后的迟到响应隔离、配置重建不重复读取、旧服务兼容、显式链接与打开失败反馈。
  阅读层保留宿主密度和链接处理器；主入口跨 599→600→599dp 后返回保留父详情的滚动位置。
  所有设备用例使用合成服务，链接用例使用模拟处理器。
- 三十六个本地 Maven 库制品、十三种独立公共 API 消费及聚合消费再次通过；新增目录服务、
  模型、图文块及展示状态已纳入消费验证。没有创建远端发布。
- 覆盖安装最终 Debug 应用后，以匿名状态从跑团详情进入完整成员列表，再打开成员详情成功；
  切换活动分区后返回保留成员选择，退出阅读层回到原跑团详情。两个公开跑团的活动列表均返回
  正常空态。只记录固定界面状态和内容存在性布尔值，没有保存条目、成员或活动的实际内容。
- 非空活动详情和成员后续页尚无本批线上样本，当前证据为公开源码、合成接口及模拟器回归。
  活动原始 HTML 保留供宿主扩展，默认渲染对不支持的结构明确提示。创建、编辑、排序、上下架、
  删除及其他管理流程仍在剩余功能清单，不因阅读接入而视为完成。

## 2026-09-20 作者主页与阅读返回

- 本机无头浏览器核对官方作者导航与各类别消息头部语义，仅读取公开脚本，没有读取真实消息
  或调用写接口。作者索引来自原有请求，发出评论与招募回应使用本人目标；社区招募列表缺少
  可证明的身份消费，保持静态作者，不能使用记录编号或为每行补请求。
- 五个可选作者导航 Provider 接入独立 App 阅读栈，支持作者主页、帖子、动态、幻化和招募详情
  逐层进入与返回。个人主页区分外部根入口与内部关系列表历史，返回已读粉丝分区不重复确认提醒。
- 全仓库五百零八项单元测试通过，无失败、错误或跳过；全仓库 Lint 与 Debug 构建通过。
  源码边界、敏感数据与负向安全自测通过，三语资源六百六十五项一致。
- 三十六个本地 Maven 库制品、十八种独立公共 API 消费及聚合消费通过。五个作者导航 Provider、
  独立动态详情、可选作者服务和资料根入口均纳入消费验证；没有创建远端发布。
- 六个功能界面共一百五十项合成设备用例通过：论坛三十五、动态十四、投影台三十二、消息十二、
  招募四十四、个人主页十三。新增作者入口、无身份降级、本人目标、四个宽度边界与返回、
  资料内部历史及分区缓存、配置重建，以及论坛/跑团楼中楼窗口暂停后恢复均覆盖。
  滚动断言区分可见条目的实际位置与 Compose 无障碍估算值，不把未放置的预组合条目当作可见内容。
- 个人主页断点按物理像素比较，避免密度换算将精确的 840dp 截断成 839dp 而误用中等布局。
  增加非整数密度下断点前一像素、精确断点以及 1920 像素对应 840dp 的单元回归。
- App 阅读栈十项合成设备用例全部通过；连同上述六个功能模块，本批覆盖一百六十项用例。
  实际系统返回覆盖来源→作者→帖子→另一作者的逐层退出；配置重建保留每层选择、滚动和请求结果。
  楼中楼进入作者页后使用物理触控确认新页面可交互，返回恢复原弹层且不重复读取回复。
  Activity 停止时模拟会话回调，恢复前已释放所有阅读 ViewModelStore 和个人资料。
- App 合成测试通过仅 Debug 存在、不可导出的通用 Activity 承载，真实服务和会话容器不启动；
  Release 合并清单不含该宿主。最终测试使用覆盖安装并指定合成测试类，没有启用线上结构探测。
- 最终构建冷启动后，模拟器账号页显示官方登录入口，未恢复可用会话。本批没有新增真实作者
  资料或消息读取结论，不推断会话变化原因；9 月 18 日的 Android 登录与读取证据仍独立保留。

## 2026-09-20 评论表情与提及编辑

- 本机无头浏览器核对官方 `actConfig.emoarr`、`Atwho`、`PostCommentFun` 与全局表单编码。
  表情范围为 1–46；候选为关注列表 `page=1&limit=5000` 的 `data.rows`；评论使用 `at-text`
  与 `uuid#character_name`，通知元数据按 `atInfo[0][uuid]` 等索引字段提交，不发送 JSON 字符串。
  主帖编辑器的标记与评论不同，没有混用，也没有凭此将主帖创作计为完成。
- 复用四层论坛模块，增加可选 `OfficialForumCommentAuthoringService`。原五参数草稿、服务
  和普通文本提交保持兼容；候选只在显式打开时读取，依赖 `AccountRead`，提交仍依赖
  `ForumWrite`。读取、授权、刷新及返回后的能力复检均由合成请求验证，没有授予新写能力。
- 全仓库五百六十项单元测试通过，无失败、错误或跳过；新增接口十三项、编辑算法和状态机
  三十九项。覆盖 UTF-16 光标、精确替换、同名不同身份、同文本粘贴、HTML 转义、重复提及、
  取消与迟到隔离、独立撤权、缓存空结果、失败保留以及上传成功后的提交重试。
- 原生编辑框使用同一份 `Editable` 承载键盘、输入法、图片 span、提及样式与无障碍动作。
  键盘删除选区不多删邻字，输入法周边删除保留选区和正在组合的文字；姓名中的表情标记按
  姓名展示。回到编辑器恢复真实原生焦点和选区；丢弃、提交成功或会话撤销清空受保护草稿。
- 论坛五十四项独立合成设备用例全部通过，包含原有三十五项和新增十九项。修复焦点后全套
  运行通过五十项，另外四项纠正子串断言后分别通过；其间生产代码未再改变。四条主流程
  将独立模拟器实际设为 599、600、839、840dp，验证图片、提及、双击仅发送一次及成功回流。
  另在实际 599dp 验证软键盘隐藏、焦点和选区恢复。截图等待目标节点与真实帧绘制，使用
  合成人名和本地图片夹具；不将图片夹具作为线上官方资源下载成功证据。
- 配置重建、回复目标保留、候选筛选不发请求、关闭取消、失败重试和撤权关闭均通过设备回归。
  `UiAutomation` 确认只有一个真实可编辑无障碍节点，`ACTION_SET_TEXT` 修改同一原生草稿。
  请求取消后的 `Idle` 不显示确认空结果；只在成功读取空列表后显示空态。
- 全仓库 Lint、Debug 构建、分层与安全门禁及负向自测通过，六百七十四项三语资源一致。
  三十六个本地 Maven 制品、十八种独立公共 API 消费及聚合消费通过；焦点修复后重新发布
  本地论坛 UI 制品并验证制品集合和对应独立消费面，没有创建远端发布。
- 本批使用空白的独立模拟器，未覆盖安装或操作保留登录现场的设备。复核保留设备时仍见
  官方登录容器，因此没有追加真实账号候选、作者主页或评论提交成功结论；历史登录证据保留。


## 2026-09-20 数据中心目录与读取基础

- 本机无头浏览器再次核对移动站入口与公开分块，五看板及鱼类、鱼饵、套装、种族四详情的
  GET、字段、单位、目录关联与本地筛选已整理为[官方页面契约](data-center-contracts.md)。
  本批没有读取真实数据中心响应，也没有调用页面挂载时的提醒确认。
- 独立 App 注入 `BundledPersonalDataCatalogProvider`，原公共服务默认值及宿主文档解码器
  保持兼容。快照来自官方公开脚本，包含三百三十五条普通鱼王、七系列五十八个零式副本、
  六百一十八套有效套装、五十六项时尚配饰和一百二十六项染剂；保留编号零的无染色，移除
  经审核的目录占位项。十三条出海特殊鱼、成就与武具分类目录尚未并入此快照。
- 目录资源为三十九万三千六百七十六字节，附来源、日期、原脚本摘要与排除规则；生成脚本
  只接收人工审核的本地字面量，不联网、不执行 JavaScript。JVM、Release AAR 的 classes.jar、
  Debug APK 及安装后的 APK 均成功读取；Release AAR 与 Debug APK 中资源的 SHA-256 均与源文件一致。
- 鉴权读取在授权查找、写入请求头、恢复授权及响应返回后复检取消和当前能力。HTTP 与业务包
  共享一次恢复预算；外部授权源异常也归一化，401/403 等认证失败不再伪装为普通刷新失败。
  取消及认证失败继续传播，普通分区错误与业务文字不作为用户可见文本。
- 状态机追踪四类请求，隔离同页切出再进入、撤权与取消后的迟到结果；普通刷新失败保留旧
  内容，目录失败允许重试。参考界面清除主看板和所有曾打开的探索缓存，子 ViewModelStore
  生命周期独立管理，配置重建不需要预先创建未访问的探索页面。
- 按官网真实渲染修正前线胜率、钓鱼成功率的比例转百分比、KDA 两位小数和前线小时单位；
  零式与投影取首行总览，前线没有 total 时不借用其他时期。非法数值不伪造为零，缺失、null、
  标量及含非法成员的数组不再当作确认空列表。抛竿次数和幻想药使用次数同步修正三语文案。
- 全仓库六百一十八项单元测试通过，无失败、错误或跳过，较上一批新增五十八项；包含目录
  七项、请求安全十八项、数值与负载十六项、状态机十五项和布局断点两项。全仓库 Lint 与
  Debug 构建通过；分层、安全门禁及负向自测通过，六百七十六项三语资源一致。
- 十九项数据中心合成设备用例全部通过：首次运行十八项通过，一项修正深层迷宫夹具分区后
  单独通过，生产代码未变。四条刷新保留用例另在实际 599、600、839、840dp 各通过一次，
  截图确认单栏与双栏、错误换行、旧内容及重试按钮可见；完成后恢复设备显示设置。
  另有一项安装 APK 内离线目录检查通过，共二十项不同设备用例，没有创建账号会话。
- 三十六个本地 Maven 制品、十八种独立公共 API 消费及聚合消费通过，新目录提供方已纳入
  独立数据模块消费检查；未创建远端发布或标签。
- 本批固定界面标签复核显示官方网页处于登录容器；关闭后原生页仍显示登录入口，重新打开
  页面未确认原生会话接受成功。没有据此推断原因或记录新的作者/个人数据线上成功结论。
  本批没有覆盖安装原登录设备，没有读取凭证或个人响应；此前有效登录证据继续独立保留。
- 目录资源及 App 注入完成不等于完整目录展示。四条结构化详情、鱼王与零式层级等语义展示、
  海钓和前线成就分区以及分享仍在[剩余功能清单](remaining-functional-scope.md)中保留未完成。

## 2026-09-20 四类数据中心详情

- 鱼类、鱼饵、种族性别、套装四条原生详情已从默认看板接入。可选领域扩展与原服务保持兼容，
  不支持扩展的旧服务不会出现新入口。四个接口保留完整数组，分类、名称查询、排序和继续显示
  均在本地执行，不猜测分页参数。此批的线上依据仍是上述官方公开脚本。
- 套装目录显示名称和物品，重复记录分别保留，部分记录不会合并为完整套装；完整进度按不同
  有效套装编号计算。目录与个人记录分别重试，缺目录或异常物品编号保持未知；种族比例乘百，
  未知比例和天数不补零。官方图标地址来自公开路径公式，图片请求不携带会话凭证。
- 修复紧凑套装详情中不可见的刷新失败，提供可见错误和重试并保留物品；套装卡片补选中语义。
  父看板、列表和物品滚动在宽度分支外保存，跨宽度返回仍保持位置。同一宿主更换服务实例时，
  同步清除并释放旧主板、探索和阅读模型，旧模型不能继续请求；同实例配置重建继续使用缓存。
- 全仓库六百六十二项单元测试通过，无失败、错误或跳过，新增四十四项：读取映射二十项、
  图标公式四项和阅读状态机二十项。数据模块共八十七项、展示状态模块四十七项；全仓库 Lint、
  Debug 构建、三十六个本地 Maven 制品、十九种独立公共 API 消费及聚合消费通过。
- 数据中心三十四项合成设备用例已全部通过，其中新增十五项四详情用例。首轮二十八项通过，
  五个用例因合成父列表不足以滚动、一项因重复关闭已关闭的键盘超时；补足专用夹具和移除
  冗余关闭后，六项重跑通过，保留全部原有导航和位置断言。
- 测试 Activity 改用与应用一致的无系统标题栏结构后，十五项阅读回归再次全部通过；随后在
  实际 599、600、839、840dp 各重复通过套装导航，并等待真实绘制帧后截图。视觉复核确认返回、
  刷新、选中套装、取得状态和单栏／双栏可见，合成图片正常加载；完成后恢复模拟器显示设置。
- 验证覆盖全量筛选不重读、目录和记录重试、部分与完整套装、未知种族字段、撤权及显式认证
  失败、服务实例替换、真实 Activity 重建、选择与滚动保留，以及旧服务不显示入口。
- 设备使用独立空白模拟器、合成记录和本地合成图片。没有覆盖安装原网页登录设备、创建会话、
  读取真实个人响应或调用提醒确认，不能把合成回归作为四详情真实账号验证。五个主看板的专用
  领域内容、完整目录关联、剩余分区及分享仍按[剩余功能清单](remaining-functional-scope.md)继续。
- 分层与源码安全门禁、负向安全自测和敏感数据边界检查通过，七百二十七项三语资源保持一致。

## 2026-09-20 钓鱼、投影与零式分区看板

- 三块看板共十四个分区已接入默认原生界面，旧服务仍兼容。鱼王关联版本和记录，海钓区分
  两条已知路线与未知编号，钓鱼成就关联目录；投影使用种族、染剂、配饰、时期及武具类别，
  零式保留系列与阶段层级。成就型阶段不猜职业和耗时，不把解除限制支持状态描述为本次开启。
- 新增可选领域扩展和分区状态机，首次进入不重复请求旧字段接口；每个分区和两组目录独立
  重试。筛选及继续显示不访问网络，刷新只重读当前看板记录，普通错误保留旧内容。认证失败
  清空兄弟页面缓存并禁用原入口，配置重建后保持失效提示；服务更换释放整个旧子模型作用域。
- 匿名本机无头浏览器核对四份官方公开脚本，状态与来源摘要见[附加目录说明](personal-data-supplementary-catalogs.md)。
  新离线目录含十三条海钓鱼、钓鱼和前线各三十五条成就、三十六个投影类别，原目录保持不变。
  两份 JSON 在 APK 与 AAR 中均与源码逐字节一致；空白设备内两个安装包目录检查通过，无联网。
- 全仓库七百四十一项单元测试通过，无失败、错误或跳过；较上一批新增七十九项：数据模块
  四十三项，含三十二项分区读取、七项目录和四项图片公式；展示状态模块三十六项，含二十五项
  状态机、十项目录关联与一项新旧看板选择兼容。数据模块共一百三十项，展示模块八十三项。
- 全仓库 Lint、Debug 构建、三十六个本地 Maven 制品、十九种独立公共 API 消费及聚合消费通过。
  分层、源码安全及负向自测、敏感数据边界检查通过；七百五十九项三语资源一致。
- 数据中心共四十六项不同的合成界面用例通过，新增十二项看板用例。首轮四十五项通过，一项
  旧认证失败用例仍等待身份卡片；更新为断言整页本地化失效提示、导航隐藏和刷新禁用后通过。
  更新合成图片后，十二项看板及该认证用例共十三项再次通过。两项安装包目录检查另行计数，
  不与界面回归重复计数。
- 设备用例覆盖十四个分区、比例和小时单位、目录缺失回退、局部重试请求数、本地筛选、
  配置重建、四详情往返、撤权、服务实例替换及旧缓存清理。本批未操作原网页登录设备，未读取
  真实个人响应或调用提醒确认；新增非空结构仍以公开源码和合成夹具为证，不新增真实账号结论。
- 实际 599、600、839、840dp 的套装分区与详情往返均通过并截图复核。视觉检查发现返回后
  分区标签可能滑出可见区，改用自动定位选中项的原生可滚动页签后，十二项看板回归及四条实际
  宽度流程再次通过；截图确认当前页签、返回、刷新、合成图标和单栏／双栏内容可见。模拟器
  显示设置已恢复，未修改已登录设备。

## 2026-09-20 前线战场完整分区

- 官方 `PersonalDataApiService` 新增可选 `PersonalDataFrontlineService`，七个 GET 对应总览、
  近七日、职业、最佳、地图、地图职业及成就；默认原生界面整合为六页，替代通用字段展示。
  旧公共服务与构造器兼容，首次选择不重复调用旧看板读取。总览和职业时期独立，地图改变时
  职业重置为全部；头部累计数据不随角色时期变化，最佳成绩的队伍顺序保留三、二、一。
  最佳与成就日期区分日历日期、本地时间及带偏移的瞬间，不给无时区值强加源时区；仅排序时
  使用显式显示时区比较，日期显示保留原始精度。
- 近七日从本地昨天往前计算七天，缺整日补零、缺字段保留未知，未知日期单独呈现。胜率及
  KDA 用总和计算，处理零分母、矛盾胜负、数值溢出和十进制转浮点边界。原生雷达使用百分制
  评分，胜率、使用比例及 KDA 百分位使用比例单位，不按字段后缀猜换算。
- 七个分区独立缓存、加载与重试；地图职业读取失败时仍保留地图总计。成就目录就绪与地图
  名称目录分别判定，旧提供方缺成就目录会显示提示和重试，目录部分刷新保留旧有效组件。
  任一认证失败清空同服务作用域的所有兄弟模型，并阻止配置重建触发自动重读。
- 本机匿名无头 Chrome 重新读取当前移动入口与前线分块，入口仍使用 `app.22e9274d.js`，
  前线分块 SHA-256 与先前公开缓存一致；三军团图片均返回 PNG。浏览器禁止业务 API 与写请求，
  图片及名称证据见[附加目录说明](personal-data-supplementary-catalogs.md)。
- 全仓八百三十项 JVM 测试通过，失败、错误和跳过均为零；较上一批新增八十九项，包括数据层
  四十七项（七接口三十九项、图片八项）、展示层三十八项和日期显示四项。全仓 Lint 与 Debug 构建通过，
  三语资源增至七百九十一项并保持一致。源码安全、负向安全自测和敏感数据边界门禁通过。
- 本批没有读取真实个人响应、确认数据提醒或执行写操作；新增非空记录目前仍以公开源码与
  合成夹具为证。原网页登录设备与此前账号验证记录保持独立，不将本批构建或模拟数据写成
  新的 Android 真实账号成功结论。
- 三十六个最终本地 Maven 制品、十九种独立公共 API 消费及聚合消费通过，包含新增前线日期
  类型；原有公共类型保持兼容。个人数据模块六十一项不同合成设备测试通过，新增十五项前线
  用例；日期与窄栏卡片修正后，十五项前线回归再次全部通过。
- 四条导航流程另在实际 599、600、839、840dp 窗口逐一通过，覆盖系统返回、选择、列表继续
  显示、配置重建和窗口切换。截图复核确认雷达标签、比例、小时及单栏／双栏内容可见；职业
  图标移至卡片标题行，避免挤窄全部指标列。七日图表同时显示缺记录零值与未知字段，日期
  范围不包含当天。测试后恢复独立模拟器的屏幕配置，未操作原网页登录设备。


## 2026-09-20 绝境战完整语义与坐标阅读

- 官方服务新增可选 `PersonalDataUltimateService`：总览摘要与队伍、职业、搭档、阶段、死亡
  五分区使用独立模型、错误和缓存，原公共类型与服务签名保持兼容。默认页面先读总览，显式
  选择副本后按需读取分区，不重复调用旧接口；733 不请求阶段。刷新同步所选副本的首通摘要，
  局部错误保留旧内容并可单独重试，会话失效与服务更换清理所有兄弟页面。
- 时间区分日期、本地时钟及偏移瞬间，未知数值不补零。职业统计表示通关次数，搭档统计表示
  共同进入次数；队伍按防护、治疗、进攻分组，同组保持输入顺序。官方七个副本封面及成就
  图片通过匿名无头 Chrome 核对，当前分块摘要与缓存一致；没有读取真实业务接口或确认提醒。
- 散点图保留官网两种坐标变换，双轴使用相同比例，不添加虚构场地底图；无效点另行提示。
  图和可访问坐标列表使用同一数据，选点与原始坐标对应，更多记录在本地展开；刷新不能把
  同一行号的新坐标当作原先选点。分享功能和真实非空账号结果仍单列，未宣称本批已覆盖。
- 全仓九百零七项 JVM 测试通过，无失败、错误或跳过，较前线批次新增七十七项：六接口
  三十三项、图片八项、展示状态与派生三十二项、日期显示四项。最终全仓 Lint、Debug 构建通过，
  三语资源共八百二十一项。
- 模拟器定位并修复两项恢复缺陷：重建首帧边距尚未就绪时，列表末尾锚点可能被提前压回；
  散点选择保存在惰性列表项内部时，离屏项不随 Activity 保存。现在在初次布局后恢复一次
  锚点，选择保存在分区级状态中；四项针对性回归通过，保留原有零点五 dp 位置断言。
  真正旋转使用显示设备旋转，恢复原方向及自动旋转设置，避免大屏忽略 Activity 方向请求。

- 最终三十六个本地 Maven 制品、十九种独立公共 API 消费及聚合消费全部通过；源码安全、
  负向安全自测、敏感数据边界及仓库布局门禁通过。个人数据七十八项不同设备用例全部通过，
  比前线批次新增十七项绝境用例，包含散点离屏后的 Activity 重建。所有记录均为合成数据。

- 599、600、839、840dp 四条流程另在实际显示窗口逐一通过，包含系统返回、配置重建、
  分区选择与展开恢复；截图复核确认单栏/双栏、当前页签、坐标轴、点位与坐标文本可读。
  结束后恢复独立模拟器的 1920×2400、360dpi 及原方向，原网页登录设备保持未操作。

## 2026-09-20 蜃景幻境武器与材料进度

- 官方页面与目录分块经本机匿名无头 Chrome 核对，五阶各二十二件，共一百一十件武器，
  科学计数法编号 51000 完整保留。新增离线资源只含公开目录与来源摘要；最终 APK 与最新
  本地 AAR 内目录字节一致。六种半魂晶、三种消幻晶、四色魔法球和透镜分段均按公开消费
  规则派生；未知材料继续显示，非法数量不开放阶段，首条未知黏土不借后条记录显示完成。
- 可选服务扩展复用同批探索八个读取，不重复读取物品或魔法球，不调用 `sysMsg/myDataOpen`。
  新状态流与旧公共探索契约分开，阶段、筛选和展开在本地完成；半魂晶历史复用原请求，返回
  恢复武器页及其父分区。目录失败可单独重试，首次来源失败不补零，局部刷新失败保留旧内容。
- 公开静态资源十八项可解码，包括尺寸 51×15 的完成标记；五阶段首件武器对应的三个不同
  图标地址均收到 HTTP 567 的 HTML 响应，浏览器以 `ERR_BLOCKED_BY_ORB` 阻止。地址公式与
  官网相同，未读取 HTML 正文，不把该结果解释为 Android 加载结论；名称和进度不依赖图片。
- 全仓九百九十三项 JVM 测试通过，失败、错误、跳过均为零，比绝境批次新增八十六项：数据
  与图片四十四项，状态与派生三十九项，日期显示三项。最终全仓 Lint、Debug 组装通过。
  三十六个本地 Maven 制品、十九类独立 API 消费与聚合消费通过；三语资源八百四十九项一致，
  源码安全、负向安全自测、敏感边界、分层和仓库结构门禁通过，静态目录生成器七项测试通过。
- 本轮所有个人记录均为合成数据，没有读取真实个人响应、确认数据提醒或执行服务器写入；
  先前网页登录及 Android 真实账号证据保持原记录，不据本轮结果补称非空武器线上验证成功。
- 个人数据模块九十三项不同合成设备用例全部通过，其中新增十五项武器用例。覆盖各阶段材料、
  本地筛选与展开、历史往返、Activity 重建、真实显示旋转、局部错误重试、目录独立重试、
  服务替换、能力撤销，以及退出探索后迟到的认证失败清理兄弟缓存。
- 设备回归修复了跨 600dp 切换布局时武器锚点丢失：五阶与父分区滚动状态提升到布局分支
  之外，历史返回后仍保持父列表位置。认证监听覆盖全部已保留探索模型，不依赖页面仍可见。
- 四条流程另在实际 599、600、839、840dp 显示窗口通过，截图复核确认单栏/双栏、阶段选择、
  材料进度、日期和操作文字可读。尺寸变化断言检查顶部完整可见锚点，切回原尺寸检查原记录；
  不要求旧视口底部记录在缩短后的视口仍可见。Activity 重建仍保留零点五 dp 位置断言。
  测试后恢复独立模拟器的默认屏幕参数，未操作原网页登录设备。

## 2026-09-20 数据中心六类原生图片分享

- 钓鱼、零式、装扮、前线、绝境战、蜃景幻境六类内容从当前已加载数据构建不可变分享文档，
  生成 PNG、预览并交给 Android 系统分享面板。必需来源失败不能生成卡片，可选来源区分空结果
  与读取失败；分享不重新读取业务接口，不确认个人数据提醒，不改变服务端状态。
- 本机匿名无头 Chrome 核对六类分享分块和样式、十四项主要资源及两个默认封面；二维码仅含
  官方公共数据中心地址。静态分享目录包含七项绝境成就、二十四个幻影职业、四十四种武器
  分类，共 5137 字节；离线生成结果、源码、最终 APK 和最新本地 AAR 的目录字节一致。
  官方远程图片未打包，图片加载失败时保留文字与进度，不将浏览器资源证据作为 Android 图片
  网络加载或真实账号内容对照证据。
- 全仓一千零一十七项 JVM 测试通过，无失败、错误或跳过；新增二十四项覆盖目录、分享派生和
  渲染生命周期。最终全仓 Lint、Debug 构建通过，三语资源八百八十项一致；三十六个本地 Maven
  制品、十九类独立公共 API 消费及聚合消费通过。仓库结构、分层、公共依赖、源码安全、敏感
  数据边界及对应负向自测通过，正式变体合并清单安全检查通过。
- 个人数据模块一百零二项不同合成设备用例通过：全量一百零一项首轮发现一项阅读鉴权回归，
  修复后阅读、缓存安全与分享共三十一项定向回归全部通过，再增加一项完整零式长图测试。
  阅读子页面继续显示自身的鉴权空态，同时父级缓存撤销，避免以父页替换已打开的阅读空态。
  本轮新增九项分享用例，包含六类 PNG 与二维码、图片失败、来源撤销、预览重建及返回。
- 完整零式合成长图包含七系列五十八个副本，实际尺寸 750×7318；最后一个副本、页脚与二维码
  均保留，二维码解码成功。另在实际 599、600、839、840dp 显示窗口逐一通过预览用例，截图
  复核确认顶部操作、文字阅读入口与限宽滚动图片可见。测试后确认独立模拟器恢复 1920×2400、
  360dpi 及原方向设置，原网页登录设备保持未操作。
- 应用平台十一项设备测试通过，覆盖私有提供器、精确只读 URI、文件撤销、准备后能力撤销、
  调用失败与取消；实际打开并取消 Android 系统分享面板，确认返回仍保留预览文件，显式关闭才清理。
  所有卡片数据均为合成数据，未选择外部接收应用，未发送真实个人内容。真实接收方延迟读取、
  非空账号六类内容对照仍作为独立验证项，不因构建、模拟器或系统面板成功而改写为已通过。

## 2026-09-20 首次签到、领奖与部队招募读取验证

- 本机匿名无头 Chrome 核对官方首页签到及奖励页的公开消费逻辑。签到提交空表单，成功结果
  使用 `totalDays`；领奖提交 `id` 和上海时区当前 `month`，官方成功分支不依赖 `data` 对象。
  本轮仅加载公开脚本，没有借匿名浏览器或已登录设备执行真实签到、领奖。
- 新增可选的单次明确操作契约，绑定当前凭证、原登录 User-Agent 和精确请求上下文。账号
  读取成功只允许用户开始尝试；有效成功响应后才授予 `DailySignIn`。取消、换号、退出、恢复
  和迟到响应均不能给另一凭证授予能力；同一凭证只读刷新保留已证实写能力。
- 账号界面首次操作先确认，取消不请求；成功后的摘要刷新失败保留成功提示并阻止当日或当月
  重复提交。日期按请求开始时锁定，跨日或跨月返回不能错误标记新周期。凭证版本参与 App
  模型清理，即使快速换号时中间会话状态被合并，也会丢弃旧确认和受保护模型。
- 写请求不使用候选主机回退、自动认证重放、重定向或网络自动重试；请求体标记为单次使用，
  防止 HTTP 408、503 的透明重发。认证失败只可触发只读会话重验，不能自动重复原写入。
- 新部队招募验证器只读取 `recruitGuildList?page=1&limit=1`，要求接受的业务状态和数组类型
  `data.rows` 才授予 `RecruitmentAuthenticated`，包括确认空数组；失败保留其他已验证能力。
  它不授予 `RecruitmentWrite`，论坛、图片上传及招募的首次写入服务与界面仍待后续接入。
- 全仓一千零六十项 JVM 测试通过，无失败、错误或跳过，本批新增四十三项。全仓 Lint、Debug
  构建与后续相关模块复验通过；三十六个本地 Maven 制品、十九类独立公共 API 消费及聚合消费
  通过，原公共服务、构造器与账号状态签名保留。三语资源八百八十五项一致，源码安全、敏感
  数据边界、负向自测、分层与正式变体合并清单安全检查通过。
- 账号七项不同合成设备用例通过，覆盖四个宽度、确认与取消、领奖、能力撤销、模型替换及
  Activity 重建。App 阅读栈十项追加回归通过：测试显式滚动来源列表，避免高视口无需滚动
  导致无效前提；楼中楼先用真实上滑展开底部弹层，再检查作者跳转、触控和返回可见性。
  此次阅读栈修正只调整测试操作，没有修改阅读栈或论坛界面的生产代码。
- 账号流程另在实际 599、600、839、840dp 显示窗口逐一通过；截图等待系统弹窗退出后采集，
  复核确认返回、登录管理、成功提示和奖励按钮可见，宽屏内容继续限制为 720dp 单栏。
  测试后恢复独立模拟器的 1920×2400、360dpi；原网页登录设备未被本批合成测试操作。
- 真实账号签到、领奖与本轮部队读取仍待当前 Android 验证。先前 Android WebView 手动登录
  和已获授权的消息读取证据独立保留，不扩展为本轮写入授权或成功结论。

## 2026-09-20 论坛、图片与招募首次明确互动

- 本机匿名无头 Chrome 再次加载官方移动首页与八个公开脚本，核对点赞/收藏的正负一结果、
  评论 ID 数组、投票驼峰字段、删除成功分支和招募联系人返回。浏览器阻止业务 API、非 GET
  及非官方请求，没有借已有登录环境尝试真实写入。证据位置见[移植记录](web-parity-plan.md)。
- 论坛与招募增加可选操作资格扩展，原有已验证写能力属性保持原义。用户明确点赞、收藏、
  发送评论、参与投票或回应才开始一次凭证绑定的操作；校验 HTTP、业务状态及成功负载后，
  分别授予论坛、上传或招募能力。失败、未知结果和身份冲突不自动重放；明确认证失败只做
  一次只读会话重验，取消继续传播。旧服务实现与公共方法签名保留。
- 选图只读取本地内容，发送才获取令牌和上传。新增可选尝试检查点在对象 PUT 前再次确认
  当前凭证，上传成功才授予独立图片能力；石之家 Cookie 和 User-Agent 不发给对象存储。
  草稿版本隔离晚到选图结果，资格撤销取消本地读取并清除受保护编辑状态。
- 招募回应仍要求已确认不是作者本人；身份未知不允许提交，保留可恢复或丢弃的草稿。
  首次身份比较使用已验证账号读取能力，旧实现已验证的招募写能力路径保持兼容。
- 全仓一千一百零九项 JVM 测试通过，无失败、错误或跳过；较上一批新增四十九项。包括真实
  会话提供器与合成传输的跨模块测试，覆盖原登录 UA、各能力独立授予、换号隔离以及令牌与
  对象上传之间的撤权。全仓 Lint、Debug 构建、正式变体合并清单安全检查通过。
- 三十六个本地 Maven 制品、十九类独立公共 API 消费及聚合消费通过；新增资格扩展与检查点
  已纳入消费者源码，未依靠聚合项目传递依赖。
- 论坛六十六项、招募四十五项及 App 阅读栈十项不同合成设备用例累计通过。回归修正了测试
  对底层无焦点窗口的误选、弹窗退出前重新点击以及过短异步等待；最终编辑器十九项、招募
  交互二十六项完整用例集和阅读栈十项分别复验通过。所有写入均使用合成服务。
- 论坛与招募八条首次互动流程已在实际 599、600、839、840dp 窗口通过，逐档截图复核。
  600dp 双栏截图暴露评论作者名被操作按钮挤压，已将作者与操作分行，并追加四档真实文字
  排版断言，检查完整可见字符、行数及省略状态；修复后四档复验和截图均通过。测试后恢复
  独立模拟器的 1920×2400、360dpi 设置。
- 用户确认 Android WebView 登录后，本次只读检查实际显示原生账号摘要。保留应用数据升级
  当前 Debug 版本并冷启动后，账号摘要仍可读取；脱敏能力摘要确认 `AccountRead`、
  `GlamourAuthenticated`、`PersonalData`、`DynamicRead`、`MessageRead` 及新增
  `RecruitmentAuthenticated` 六项读取能力为真。`DailySignIn`、`ForumWrite`、
  `ForumImageUpload`、`RecruitmentWrite` 为未验证，没有执行真实写入。核对工具只输出固定
  界面标签，未导出凭证、个人响应或账号截图；能力结果不扩展为全部具体页面已逐项复验。

## 2026-09-20 我的部队与相册阅读

- 本机匿名无头 Chrome 核对部队资料、成员、动态、相册、照片及评论的公开脚本和八条 GET
  契约。匿名读取正确 API 主机下的 `userInfo/getUserBasicInfo` 返回 HTTP 200、业务码
  `10403`；页面主机与 API 主机分开记录，不将页面主机的错误路径当作匿名接口行为。
- 新增 `guild-domain`、`guild-data`、`guild-presentation`、`guild-ui-compose`，通过独立
  `GuildRead` 及可选账号导航接入 App。消息中的照片 ID 可直接打开详情，再进入真实社区
  UUID 对应的作者页面；返回不重复读取消息。未注册成员不显示作者跳转。
- 设备内五条已审查只读请求均返回 HTTP 200、业务码 `10000`，当前成员有数据、动态与相册
  为空。资料字段检查确认住宅公开值可为布尔型，等级不满足严格整数形状；等级依官网
  `parseInt` 消费方式兼容，合成夹具覆盖前缀和空值，人数与身份仍严格校验。
- 全仓 1148 项 JVM 测试通过，无失败、错误或跳过；新增部队领域、数据、展示与断点测试
  分别为 2、18、17、1 项。全仓 Lint、Debug 构建及正式变体合并清单安全检查通过。
  分层、公共依赖、源码安全、敏感数据边界及负向自测通过，929 项三语资源一致。
- 40 个本地 Maven 制品、22 类独立公共 API 消费及聚合消费通过。新增消费覆盖部队数据和
  界面、可选账号部队导航；旧账号屏幕签名保持兼容，未增加远端仓库或私有依赖。
- 部队界面 11 项、App 阅读栈 14 项、账号入口 1 项及能力摘要 7 项合成设备用例通过。
  覆盖四档宽度、图片实际解码、消息到照片与作者返回、配置重建、列表和楼中楼滚动保持、
  被覆盖层停止交互及会话撤销。图片加载使评论按钮移出视口时，测试先等待解码，再滚动
  至目标点击；未以缩小生产图片代替真实滚动验证。
  阅读栈返回测试改为向当前前台窗口发送系统返回键，避免 Espresso 误选弹层下的无焦点
  Activity；修改后完整 14 项复验通过，生产导航保持不变。
- 599、600、839、840dp 实际显示窗口的照片导航逐档通过并复核截图。599dp 单栏返回相册，
  600dp 起列表与详情双栏，840dp 调整两栏比例并限制内容宽度。图片、作者与评论正常显示；
  设备测试使用本地合成图片，没有给测试包增加网络权限。验证后恢复独立模拟器显示设置。
- 保留 Android WebView 登录数据升级并冷启动后，原生账号部队入口、资料、已注册与未注册
  成员、动态空态、相册空态及返回账号页均通过。`AccountRead`、`RecruitmentAuthenticated`、
  `GlamourAuthenticated`、`PersonalData`、`DynamicRead`、`MessageRead`、`GuildRead` 为真；
  `DailySignIn`、`ForumWrite`、`ForumImageUpload`、`RecruitmentWrite` 仍未验证。
- 真实账号未加入部队分支、非空动态、非空相册、照片详情与评论/楼中楼仍缺本轮线上样本，
  已完成的源码和合成验证不能替代它们。上传、编辑、点赞、评论、招待和删除仍是后续范围；
  本轮没有执行真实写入或消息未读确认，没有导出凭证、实际值、个人响应或账号截图。


## 2026-09-20 部队资料编辑与照片操作

- 基于本机浏览器保存的官方公开 SPA 脚本完成七个操作端点和三个图片上传 channel 的契约
  实现。`getCharacterBindInfo?platform=1` 与部队标签目录经 Android 会话只读验证，均为
  HTTP 200、业务码 `10000`；只保留字段类型、空/非空及关系布尔结果，不记录真实值。
- `GuildApiService` 增加独立操作契约；新增封闭资料更新类型、图片上传服务、资格关系、
  可选 Compose 操作提供器，保留既有读取 API。原生界面覆盖头像裁剪、住宅公开、标签、
  平日/周末活跃时间、简介、相册上传、照片点赞/删除、评论/回复及删除本人评论。
- 新增 `GuildWrite` 和 `GuildImageUpload`，以已验证 `GuildRead` 为首次明确尝试前提。
  只读身份资格不授予写能力；令牌获取仅在用户明确上传后执行。所有提交与草稿绑定原
  凭证及读取能力世代；换号、撤权再恢复、资源切换和迟到回调均不能复活旧草稿或结果。
- 上传按张延迟读取，最多九张；同一作用域内的明确重试复用成功对象。裁剪支持拖动、缩放、
  辅助滑块和旋转恢复，EXIF 方向先纠正，预览与导出共享坐标。显式声明工程已有解析版本的
  AndroidX ExifInterface，避免依赖旧平台实现或偶然传递依赖。
- COS 传输不继承 WebView 登录头、默认头、CookieJar 或拦截器，默认实现禁止自动重放。
  使用无代理回环服务器验证实际收到的请求头隔离；服务假对象另覆盖三种 channel、令牌
  字段、上传前后撤权、身份冲突及最终登记失败重试。
- 全仓 1211 项 JVM 测试通过，无失败、错误或跳过；全仓 Lint、Debug 构建通过。方向解析
  替换后重新通过目标模块单测、Lint、测试包和 App 构建。40 个公共 Maven 制品、22 类
  独立 API 消费与聚合消费通过，包含新作用域、操作服务、上传服务和界面提供器。
- 部队及裁剪 18 项、App 阅读栈 14 项、能力摘要 9 项合成设备用例通过。覆盖显式提交/取消、
  删除确认、评论目标、相册本地草稿、像素裁剪、EXIF 方向、配置恢复、返回栈及能力撤销。
  599、600、839、840dp 各执行照片导航和明确操作用例，逐档截图复核通过，显示配置已恢复。
- 真实 Android 会话保留数据升级并冷启动后，账号部队入口、资料、已注册/未注册成员、动态
  空态、相册空态、上传入口资格和返回账号验证通过。七项读取能力为真；`GuildWrite`、
  `GuildImageUpload` 与既有四项写能力均仍为未验证。没有执行真实保存、上传、点赞、评论
  或删除，也没有自动清除消息提醒。真实权限矩阵、写入成功样本和非空照片/评论仍待验证。

- 最后一次升级的恢复过程一度停留登录入口，重新打开官方 WebView 后自动恢复既有登录，
  无需重新输入凭证；随后独立冷启动的原生账号及部队入口通过。该次现象未取得根因证据。
  源码复核另确认恢复阶段将所有异常都删除凭证，现已改为仅明确认证拒绝才删除，临时网络、
  HTTP 503 和响应解析失败保留加密及网页会话，同时不提供内存授权。新增两项单测覆盖保留、
  授权关闭和恢复重试，原有明确失效清理测试继续通过；不能将此合成证据写成当次线上根因。

## 2026-09-22：动态原生互动与会话边界

- 本机无头移动 Chrome 读取官方公开 SPA 与详情分块，确认正文点赞、根/子评论、单图和
  本人内容删除的调用。详情明确关闭评论点赞，未将共享的帖子评论点赞接口用于动态。
- 新增可选操作服务与独立图片服务，保留原阅读接口、实体构造与界面签名。成功码、正负一
  点赞结果、DELETE 表单、父子 ID、提及编码和身份读取均有合成接口覆盖。
- `DynamicWrite`、`DynamicImageUpload` 只在各自明确操作成功后授予。草稿、上传结果和
  删除资格绑定同一凭证与读取能力世代；读取能力撤销后恢复不能复活旧作用域。换号时清除
  会话专属 ViewModelStore，覆盖两账号均可读取、不可取消的迟到图片和旧稿隔离。
- 原生编辑器支持 46 个官方表情与有身份边界的提及。评论和正文阅读保留内嵌表情，纯表情
  评论不再显示为空。单图读取限制字节数、校验类型、缩放预览并校正 EXIF；明确发送时才上传。
- 全仓 1248 项 JVM 测试通过，无失败、错误或跳过；全仓 Lint、Debug 构建通过。仓库分层、
  公共依赖、安全门禁及负向自测通过，1014 条三语资源一致。
- 动态 31 项、能力摘要 13 项合成设备测试通过；最终 App 回归 28 项通过，2 项需显式启用的
  真实接口结构测试按设计跳过，不能计为线上成功。覆盖取消、明确回复/删除、提交失败保稿、
  配置重建及发送中重建不重复提交、图片方向、真实表情绘制、宽度变化与阅读返回。
  599、600、839、840dp 实际窗口逐档通过打开、输入、取消，编辑器截图无裁切，显示配置已恢复。
- 40 个公共 Maven 制品验证、22 类独立 API 消费和聚合消费通过，包含动态操作、图片服务及
  可选界面提供器。仅生成本地制品，未创建远端发布。
- 用户补充确认 Android 官方 WebView 登录后返回原生账号页；此前真实读取证据继续保留。
  本批设备回归使用合成服务，没有新增真实账号点赞、评论、上传、删除或消息已读操作。
  当前真实权限、非空评论/楼中楼及写入成功结构仍保留待验证。


## 2026-09-22：动态发布与帖子/攻略转发

- 本机无头移动 Chrome 核对官方发布与转发页面，确认两个表单端点、三档范围、九图限制、
  发布表情 HTML、空转发默认内容及 `channel=dynamic`；没有向真实账号执行写操作。
- 原生发布支持提及、官方表情、有序图片、明确丢弃、失败保留与重试。逐张延迟读取图片，
  同作用域缓存成功上传；评论和发布的上传结果按用途隔离。帖子/攻略转发使用已读取的来源，
  不接受图片，不猜测成功返回 ID。App 新建入口与论坛可选转发回调均已接入。
- 配置重建保留草稿和提交中的同一实例；重复点击只发送一次。凭证变化清空目的地容器，
  认证失败时连同来源标题清除。成功只关闭编辑器一层并刷新动态一次，旧帖阅读位置保留。
- 全仓 1265 项 JVM 测试通过，无失败、错误或跳过；全仓 Lint 与 Debug 构建通过。
  仓库分层、公共依赖、安全门禁及负向自测通过，1041 条三语资源一致。
- 动态完整 46 项合成设备用例通过；论坛累计 67 项通过；App 原有 28 项及新增 2 项通过，
  另 2 项需明确启用的真实接口测试按设计跳过。论坛旧宽度测试在手机上受窗口约束，修正
  测试密度后六项布局复验通过；没有修改生产布局。发布表情测试使用本地图片假服务，避免
  无网络权限的测试包联网退出。上述问题修正后分别完整或针对性复验，不隐去首次失败。
- 599、600、839、840dp 实际窗口分别通过原生输入、范围切换与明确发布，逐档截图复核；
  中、大窗口限制内容宽度，模拟器已恢复 1080×2400、420dpi。模拟服务没有写入官方账号。
- 40 个公共 Maven 制品、22 类独立 API 消费及聚合消费通过，包含新增发布、图片服务、
  动态导航、论坛转发与发布屏幕。接入文档明确成功回调与返回的调用顺序，避免宿主重复返回。
- 已补充五类招募独立转发与幻化投稿同步的公开源码证据，后续原生范围仍明确列出。
  本轮真实发布、上传、转发、范围和权限验证均未执行；此前用户确认的 Android WebView
  登录及只读证据不等同于本轮写能力成功。

## 2026-09-22：五类招募分享到动态

- 本机匿名无头移动 Chrome 核对新人、副本、部队、跑团和其他招募详情。五类入口均只确认
  可见范围，使用独立招募转发表单；准确保留 `recruid_id` 拼写，不附加帖子正文、图片或提及。
  原生详情与独立详情已接入可选导航，App 映射五类来源，成功后仅关闭确认页并刷新动态一次。
- 新增可选服务，不扩充旧发布服务的抽象方法。操作绑定当前动态作用域，覆盖错误、取消、
  迟到返回、明确重试与不自动重放；配置重建保留范围及同一次提交，凭证变化清理来源标题。
- 复核修正部队读取门禁：`hasCommunityIdentity` 只依据 `RecruitmentAuthenticated`。
  保留 `RecruitmentWrite` 不能继续读取部队或保留部队转发来源；App 两处统一使用实际会话
  能力映射。数据回归验证撤销读取后零请求，导航回归验证部队容器清除而其他招募保持可用。
- 全仓 1280 项 JVM 测试通过，无失败、错误或跳过；全仓 Lint、Debug 构建通过。仓库分层、
  公共依赖、安全门禁及负向自测通过，1045 条三语资源一致。
- 新增动态转发确认与配置重建共 10 项合成设备用例通过；599、600、839、840dp 实际窗口
  逐档操作与截图复核通过，未发现裁切，显示配置恢复。设备用例使用合成来源与服务。
- 招募交互 28 项、实际 App 宿主回流 3 项设备用例通过，覆盖五种来源、只读旧接入和成功
  返回一层。首次合并运行把另一个测试包的类名也传给 runner，出现找不到测试类的启动错误；
  分为两个任务后全部通过，没有把该启动失败计作功能验证成功。
- 40 个公共 Maven 制品、22 类独立 API 消费与聚合消费通过，包含新增招募转发服务、
  草稿、确认界面与可选导航提供器；未生成远端发布。
- 本轮未调用真实账号转发接口，三种可见范围的实际发布结果与账号写权限仍待独立验证。
  既有 Android WebView 登录成功及读取证据不等同于真实转发成功。

## 2026-09-22：论坛标题与正文搜索

- 本机匿名无头移动 Chrome 核对官网九种搜索分类，确认论坛搜索类型为帖子标题 1、帖子正文
  2、攻略标题 3、攻略正文 4。旧实现把列表的攻略类型 2 用于搜索，现已修正；列表仍保持 2。
  新增可选搜索扩展和原生字段选择，保留旧服务、查询和状态数据类构造，旧入口默认标题。
- 匿名 type 1/3 返回 HTTP 200、10000 和仅含 `rows` 的负载；type 2/4 本次传输超时，不能
  记为鉴权拒绝或线上成功。源码确认搜索端点业务码 10003 表示结束为空，原生仅在该端点
  采用此分支，零授权刷新；其他读写端点的认证策略保持不变，该码仍无本次线上样本。
- 状态机保留字段、已提交关键词和分页快照。字段切换或清空首次搜索时，取消及不可取消的
  迟到响应均不能覆盖新结果；普通刷新失败保留旧内容。旧服务不展示不支持的字段选择。
- 全仓 1292 项 JVM 测试通过，无失败、错误或跳过；全仓 Lint、Debug 构建、仓库门禁与
  安全负向自测通过，1047 条三语资源一致。40 个公共 Maven 制品、22 类独立 API 消费及
  聚合消费通过，覆盖新增搜索接口和独立展示状态。
- 搜索 8 项合成设备用例累计通过，覆盖四种组合、旧服务、分页重置、四档宽度和真实
  Activity 重建。首次测试包编译发现多余断言导入，移除后重新完成构建；两个输入框断言
  将标签与输入值一起比较而失败，完整语义证明输入未丢失，改为检查 `EditableText` 后两项
  复验通过。没有修改生产逻辑掩盖测试问题，也未将首次失败记为通过。
- 599、600、839、840dp 实际窗口逐档选择并截图复核通过，字段组和输入区均无裁切；紧凑
  单栏、中等和展开双栏保持原行为，显示配置已恢复为 1080×2400、420dpi。
- 用户搜索和统一搜索入口仍待原生接入，幻化目录继续遵守其独立鉴权门禁。本批没有调用
  真实账号写接口，也没有自动确认消息提醒。
