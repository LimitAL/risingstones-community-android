# 安全说明

## 报告安全问题

不要在公开议题中提交 Cookie、令牌、请求授权头、个人数据响应、设备标识或可复现的账号接管
细节。

请优先使用 GitHub 仓库的私密漏洞报告功能。如果该功能不可用，请通过仓库资料页公布的联系
方式私下联系维护者，并只提供建立安全沟通渠道所需的最少信息。

报告应说明受影响的模块和版本、被突破的安全边界，以及经过脱敏的复现步骤。不得附带真实的
`ff14risingstones` Cookie。

## 支持范围

首次正式发布前只维护当前 `main` 分支。稳定版本发布后，将在此处补充受支持的版本范围。

欢迎报告以下方面的安全问题：

- 凭证存储与删除。
- WebView 导航和 Cookie 捕获。
- 请求授权与官方接口传输。
- 备份、换机迁移和导出组件边界。
- 意外引入非项目范围的服务或敏感配置。

官方服务本身的可用性或普通行为变化不属于客户端漏洞，除非它导致用户数据泄露或绕过客户端
安全边界。

预期凭证边界为：`noBackupFilesDir` 内的 AES-GCM 密文、不可导出的 Android Keystore
密钥、不记录凭证日志，并禁止备份和设备迁移。默认登录界面还应在 WebView 可见期间阻止截图
和最近任务缩略图，同时保留接入方 Activity 原有的 `FLAG_SECURE` 状态。
